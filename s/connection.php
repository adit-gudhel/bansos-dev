<?
/*
//Define Database Connection
$dbhostname		= "localhost";
$dbusername		= "root";
$dbpassword		= "root";
$dbname			= "spktdb";
/
if (!preg_match("/admin/", $PHP_SELF)) {
	foreach ($_POST as $key=>$val) {
		$_POST[$key] = addslashes($val);
		$$key = addslashes($val);
	}
	foreach ($_GET as $key=>$val) {
		$_GET[$key] = addslashes($val);
		$$key = addslashes($val);
	}
}
*/
?>